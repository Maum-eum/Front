import { BrowserRouter as RouterProvider } from 'react-router-dom'
import Router from './utils/Router';


function App() {
  return (
    <RouterProvider>
      <Router/>
    </RouterProvider>
  )
}

export default App
